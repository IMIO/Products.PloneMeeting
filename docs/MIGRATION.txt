Migration to PloneMeeting 3.3 :
===============================

1) Run Plone migration to Plone 4.3.4;
2) Run upgrade step for collective.iconifieddocumentactions;
3) Run upgrade step for Products.PloneMeeting;
4) Run upgrade step for the PM subplugin;
5) Check some elements :
   - certified signatures on each MeetingConfig;
   - adapt certified signatures call on relevant templates;
   - generate every templates to check for consistency;
   - MeetingItem.listProposingGroup is now MeetingItem.listProposingGroups;
   - MeetingGroup._createPloneGroup is now MeetingGroup._createOrUpdatePloneGroup;
   - ToolPloneMeeting.isManager now need a context as first mandatory argument now that MeetingManagers are local;
   - MeetingItem.getAdviceDataFor now needs a mandatory first paramter, the item itself (avoid calls TTW).
